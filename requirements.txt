anthropic>=0.96.0
claude-agent-sdk>=0.2.0  # backend: claude_code — also requires the Claude Code CLI
numpy>=1.26  # shared/textstats.py, shared/embeddings.py, evals/diversity.py
pandas>=2.2
pyyaml>=6.0
python-dotenv>=1.0
tenacity>=8.2
streamlit>=1.40
faker>=30  # seeded fictional people/org name pools (shared/entity_pools.py)
openai>=1.40  # embeddings for the semantic diversity eval (shared/embeddings.py)
tiktoken>=0.7  # token-accurate truncation for embeddings (shared/embeddings.py)
wordfreq>=3.0  # background English frequencies for the phrase-tic audit (evals/audit_dad.py)
huggingface_hub>=0.25  # dataset publishing (evals/publish_hf.py)

# Test-only (inert outside pytest runs)
pytest>=8.0
pytest-socket>=0.7
